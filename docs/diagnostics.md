# Diagnostics

`python -m track_it diagnostics` reports OS, Python, memory, PyTorch/CUDA capability, FFmpeg
version/configuration, and offline asset presence. Paths are omitted by default. Crash reports
must be reviewed before sharing and must never include videos, model weights, credentials, or
prompt/mask data.
