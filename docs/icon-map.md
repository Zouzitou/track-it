# Material Symbol map

All icons are pinned, offline Google Material Symbols SVGs. Outlined style is normal; checked
state and semantic tint indicate active tools. Positive point maps to `ads_click`, negative
point to `do_not_disturb_on`, export to `upload_file`, and anchor to `location_searching`.
Missing assets render a visible `?` fallback and a development warning, never a blank button.
