# Contributing

Open an issue before large changes. Keep inference local, preserve the layer direction, use
typed domain models, pass argument arrays to subprocesses, and include deterministic tests.
Do not commit model weights, videos, projects, caches, logs, or credentials. Run
`scripts/test.ps1` (Windows) or `scripts/test.sh` (Linux) before submitting a pull request.
